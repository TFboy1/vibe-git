{
  "name": "@vibe-git/host",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run test/v20.test.ts test/v21-contract.test.ts",
    "build": "tsc -p tsconfig.build.json"
  },
  "dependencies": {
    "@fastify/cors": "^11.1.0",
    "@vibe-git/protocol": "0.1.0",
    "fastify": "^5.6.0"
  },
  "devDependencies": {
    "tsx": "^4.20.5"
  }
}
